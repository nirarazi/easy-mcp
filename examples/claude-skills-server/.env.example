# Google Gemini API Key (Required)
# Get your API key from: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your-google-api-key-here

# Skills Directory (Optional)
# Path to the directory containing SKILL.md files
# Default: ./skills (relative to the project root)
SKILLS_DIR=./skills

# LLM Model (Optional)
# The Gemini model to use
# Default: gemini-1.5-flash
LLM_MODEL=gemini-1.5-flash

# System Instruction (Optional)
# Custom system instruction for the LLM
# Default: Basic instruction about Claude Skills
SYSTEM_INSTRUCTION=You are a helpful assistant with access to Claude Skills.

# VectorDB Configuration (Optional)
# Endpoint for the VectorDB service used for RAG
VECTOR_DB_ENDPOINT=https://your-vectordb.com
VECTOR_DB_COLLECTION=documents

# Retrieval K (Optional)
# Number of documents to retrieve for RAG
# Default: 3
RETRIEVAL_K=3

# Firebase Configuration (Optional)
# If using Firestore for persistence, provide your Firebase config
# This should be a JSON string with your Firebase configuration
# Example: {"projectId":"your-project","apiKey":"your-key",...}
FIREBASE_CONFIG=

# Firebase Auth Token (Optional)
# Authentication token for Firebase
FIREBASE_AUTH_TOKEN=
